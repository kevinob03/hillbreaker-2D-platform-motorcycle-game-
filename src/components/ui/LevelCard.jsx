import { Flag,Gauge,LockKeyhole,MapPin,Trophy } from 'lucide-react'; import ArcadeButton from './ArcadeButton'
export default function LevelCard({level,index}){return <article className={`level-card ${level.theme} ${level.unlocked?'':'locked'}`}><div className="level-art"><b>0{index+1}</b><i/><span/></div><div className="level-body"><header><div><em>{level.difficulty}</em><h2>{level.name}</h2></div>{!level.unlocked&&<LockKeyhole/>}</header><p>{level.description}</p><div className="level-stats"><span><MapPin/> {level.bestDistance.toLocaleString('es-ES')} m</span><span><Trophy/> {level.bestScore.toLocaleString('es-ES')} pts</span><span><Gauge/> Consumo x{level.fuelConsumption}</span></div>{level.unlocked?<ArcadeButton to={`/game/${level.id}`} icon={Flag}>Correr</ArcadeButton>:<button className="arcade-button disabled" disabled><LockKeyhole/>Bloqueado</button>}</div></article>}
