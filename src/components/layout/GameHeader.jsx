import { Bike,CircleHelp,House,Menu,Trophy,X } from 'lucide-react'
import { useState } from 'react'; import { NavLink } from 'react-router-dom'
const links=[['/','Inicio',House,true],['/game','Pistas',Bike],['/scores','Puntajes',Trophy],['/how-to-play','Cómo jugar',CircleHelp]]
export default function GameHeader(){const[open,setOpen]=useState(false);return <header className="game-header"><NavLink className="brand" to="/" onClick={()=>setOpen(false)}><span className="brand-mark">●</span><b>Hill<span>breaker</span></b></NavLink><button className="menu-toggle" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-label="Abrir menú">{open?<X/>:<Menu/>}</button><nav className={open?'main-nav open':'main-nav'}>{links.map(([to,label,Icon,end])=><NavLink key={to} to={to} end={end} onClick={()=>setOpen(false)}><Icon size={16}/>{label}</NavLink>)}</nav><span className="phase-chip">Fase 1</span></header>}
